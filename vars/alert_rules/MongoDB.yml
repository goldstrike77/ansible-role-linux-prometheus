---
prometheus_mongodb_alert_rules:
# MongoDB database
  - alert: MongoDB
    expr: "mongodb_up == 0"
    for: 5m
    labels:
      severity: critical
    annotations:
      description: "Instance has been stoped"
