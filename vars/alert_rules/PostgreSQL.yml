---
prometheus_postgresql_alert_rules:
# PostgreSQL database
  - alert: PostgreSQL
    expr: 'pg_static{job="PostgreSQL"} < 1'
    for: 5m
    labels:
      severity: critical
    annotations:
      description: "Instance has been stoped"
